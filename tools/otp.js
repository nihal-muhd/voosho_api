export const doSms = (data) => {
    // Sending otp via sms to user for particualr phone number or email using a third party tool like twilio
    return 'otp generated'
}

export const verifyOtp = (otp) => {
    // verify entered otp by user
    return 'otp verified'
}